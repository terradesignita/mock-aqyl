# Консилиум: визуальный редизайн (цвет, персоны, раскладка, движение)

## Контекст и цель

Функциональный редизайн Консилиума в живой групповой чат уже сдан (`docs/superpowers/specs/2026-08-04-council-live-chat-design.md`, `docs/superpowers/plans/2026-08-04-council-live-chat.md`). Пользователь оценил результат как «скучный» — не по функциям, а по внешнему виду: интерфейс использует нейтральную серо-белую палитру дизайн-системы почти без цвета, персоны различаются только тонкой левой полоской и кружком с инициалами, раскладка — типовой шаблон «сайдбар + чат», а движение ограничено fade-in и точками набора текста.

Цель этого документа — зафиксировать визуальный язык для той же функциональности: цвет и характер персон становятся ведущим визуальным элементом, а не декором поверх нейтрального шаблона. Все решения ниже утверждены пользователем через визуальный компаньон (мокапы в браузере) и текстовые уточнения — см. цепочку решений в конце документа.

**Не входит в этот документ:** углубление контента диалогов (расширение keyword-правил `buildFollowUpReplies`, реакции персон друг на друга в тексте) — это отдельная, параллельно согласованная задача по данным, не по визуалу.

**Правки после аудита (`better-interface`, full mode) от 2026-08-04:** аудит нашёл 2 HIGH-риска в первой версии этого документа ещё до реализации — заниженный контраст рамки в тёмной теме и скученность 4 цветов персон в одной узкой полосе оттенка. Разделы 1-4 ниже переписаны с учётом обоих; исходные версии не сохраняются, эти разделы — источник истины.

## 1. Данные персон: `hex` вместо Tailwind-класса

`CouncilPersona` (`src/data/council.ts`) меняет поле `color` (Tailwind bg-*-класс) на `hex` — единственный источник цвета персоны. Причина отказа от Tailwind-класса: цвета 4 персон ниже подобраны точечно под конкретные WCAG-пороги (см. таблицу), и они не совпадают ни с одним стандартным оттенком палитры Tailwind — держать их как «ближайший» именованный класс было бы неточным компромиссом без выгоды, раз всё равно нужен raw-hex для градиента обложки (§5) и CSS-переменных (§2).

```ts
export interface CouncilPersona {
  id: string;
  name: string;
  initials: string;
  role: string;
  inspiredBy: string;
  /** Основной цвет персоны — hex. Используется в обеих темах для солидной
   *  заливки (аватар, тег) и как база для рамки/кольца (см. §2). */
  hex: string;
  /** Только у персон, чей `hex` не проходит WCAG 3:1 (не-текстовый порог)
   *  на тёмной карточке (`--card` в `.dark`) — более светлый вариант ТОЛЬКО
   *  для рамки/кольца в тёмной теме. У остальных `hex` уже проходит порог
   *  в обеих темах, отдельного значения не нужно. */
  darkHex?: string;
  /** Короткое слово-тег архетипа для чипа под именем персоны. */
  tag: string;
}
```

Каждое значение в таблице ниже проверено скриптом (OKLCH-конверсия + WCAG-контраст, тот же метод, которым в этой кодовой базе уже поправлены `--success`/`--destructive`): белый текст на `hex` (аватар/тег) и `hex`/`darkHex` на 3:1 против тёмной `--card` (`oklch(0.221 0.037 258.8)`, реальная рамка на 100% непрозрачности — см. §2, почему именно 100%, а не тонированная).

| id | hex | white-text | dark-контраст рамки | darkHex | tag |
|---|---|---|---|---|---|
| founder | #a75d00 | 4.99:1 | 3.46:1 (сам hex) | — | Визионер |
| operator | #7c3aed | 5.70:1 | 3.03:1 (сам hex) | — | Оператор |
| engineer | #2563eb | 5.17:1 | 3.34:1 (сам hex) | — | Инженер |
| contrarian | #0f766e | 5.47:1 | 3.16:1 (сам hex) | — | Скептик |
| industrialist | #c34700 | 4.97:1 | 3.47:1 (сам hex) | — | Промышленник |
| product | #c026d3 | 4.71:1 | 3.67:1 (сам hex) | — | Продакт |
| brand | #c13892 | 4.94:1 | 3.50:1 (сам hex) | — | Бренд |
| platform | #4f46e5 | 6.29:1 | 2.75:1 ❌ | #5954f3 (3.28:1) | Платформа |
| competitor | #ce3452 | 4.96:1 | 3.49:1 (сам hex) | — | M&A |
| resilience | #0e7490 | 5.36:1 | 3.23:1 (сам hex) | — | Устойчивость |
| scale | #047857 | 5.48:1 | 3.15:1 (сам hex) | — | Масштаб |
| transform | #57534e | 7.63:1 | 2.27:1 ❌ | #6f6b66 (3.27:1) | Трансформация |

Изменились 4 значения `hex` относительно первой версии документа — `founder` (#b45309→#a75d00), `industrialist` (#c2410c→#c34700), `brand` (#be123c→#c13892), `competitor` (#b91c1c→#ce3452). Это те самые 4 персоны, которых аудит нашёл в одной 32°-полосе оттенка (H17-49, все почти одинаковой светлоты) — теперь они разведены до H15/45/70/345, шаг ≥25° между соседями, при этом каждая всё ещё проходит оба контрастных порога из таблицы. `operator`/`engineer`/`contrarian`/`product`/`resilience`/`scale` не менялись — их исходный `hex` уже проходил 3:1 на тёмной карточке. `platform` и `transform` — единственные, кому нужен `darkHex` (их светлота недостаточна для 3:1 при любом развороте оттенка без ухода в другой цвет, поэтому не переоттеночены, а получили более светлый вариант только для тёмной темы).

`hex`/`darkHex`/`tag` — данные, не поведение: ничего в `buildOpeningMessages`/`buildFollowUpReplies` их не читает, guardrail-тест на реальные имена лидеров их не касается.

## 2. Cтили персоны: CSS-переменные вместо Tailwind-палитры

Раз цвет — произвольный `hex`, а не именованный Tailwind-оттенок, статическая карта `color → класс` (как в текущем `PERSONA_BORDER_CLASS`) не подходит — вариантов слишком много для перечисления, и это была бы точная копия данных из §1. Вместо этого: **рамка/кольцо всегда на 100% непрозрачности** (не тонированная — тонирование ниже 100% было первой версией этого документа и именно оно провалило дедлайн-проверку аудита: даже при 70% непрозрачности ни один из 12 цветов не держит 3:1 против тёмной карточки, потому что светлота самого тонирования падает вместе с непрозрачностью). Только это и объясняет, почему таблица в §1 проверяет `hex` на 100%, а не на какой-то процент.

Два статических класса-константы в `src/routes/council.tsx` (JIT-безопасны — это литералы с `var()` внутри, не интерполяция цвета в строку класса):

```ts
/** Рамка/кольцо персоны — 100% непрозрачности, свой цвет в каждой теме. */
const PERSONA_BORDER_CLASS = "border-[var(--persona-color)] dark:border-[var(--persona-color-dark)]";
/** Лёгкая заливка фона персоны — единственное место с непрозрачностью <100%,
 *  т.к. это декоративный фон, а не элемент, несущий идентичность (см. §4). */
const PERSONA_TINT_CLASS = "bg-[color-mix(in_oklab,var(--persona-color)_10%,transparent)]";

function personaColorVars(p: CouncilPersona): React.CSSProperties {
  return {
    "--persona-color": p.hex,
    "--persona-color-dark": p.darkHex ?? p.hex,
  } as React.CSSProperties;
}
```

`personaColorVars(p)` ставится через `style` один раз на общем предке (обёртка группы сообщений персоны, см. §4) — CSS-переменные наследуются вниз по DOM, поэтому и аватар, и каждый пузырь сообщения в группе используют один и тот же `var(--persona-color)`/`var(--persona-color-dark)` без повторной передачи `style`.

## 3. Идентичность персон: кольцо + тег роли

`PersonaAvatar` (`src/components/PersonaAvatar.tsx`) получает новый опциональный проп `ringClassName?: string` — рисует `border-2` вокруг круга поверх текущего фона:

```tsx
export interface PersonaAvatarProps extends HTMLAttributes<HTMLSpanElement> {
  initials: string;
  size?: keyof typeof SIZE_CLASS;
  ring?: boolean;
  /** Кольцо цвета персоны — новое, для варианта C идентичности персон.
   *  Значение — константа PERSONA_BORDER_CLASS (§2), одна и та же для всех
   *  персон; фактический цвет приходит через CSS-переменные в `style`
   *  (personaColorVars(p) на предке или напрямую на этом элементе). */
  ringClassName?: string;
}
```

Фон аватара (`className={...}` с `backgroundColor` через inline-style или `style={{ backgroundColor: p.hex }}` — раньше был Tailwind-класс `p.color`, теперь просто hex) не зависит от темы: контраст с белым текстом инициалов проверен на исходном `hex` в §1, тема на это не влияет. `ringClassName` — всегда `PERSONA_BORDER_CLASS`, а конкретный цвет подставляется через унаследованные `--persona-color`/`--persona-color-dark` (см. §2). Существующий `ring?: boolean` (белая обводка для стека аватаров) остаётся отдельным пропом — оба класса применяются одновременно через `cn()`, если заданы оба.

Тег роли — новый маленький компонент прямо в `council.tsx` (не отдельный файл, слишком мал):

```tsx
function PersonaTag({ tag, hex }: { tag: string; hex: string }) {
  return (
    <span
      className="rounded-full px-2 py-0.5 text-[9px] font-extrabold uppercase tracking-wide text-white"
      style={{ backgroundColor: hex }}
    >
      {tag}
    </span>
  );
}
```

Фон тега — сплошной цвет персоны (не tint), белый текст поверх — контраст закрыт тем же принципом, что уже используют `PersonaAvatar` (белый текст на насыщенном фоне из палитры amber-700/violet-600/etc., все с достаточной светлотой для 1000/700/600-оттенков Tailwind).

**Где используется (кольцо + тег вместе):** список выбора персон в `NewCouncilPanel` (шаг 1) и `PersonaPicker` — обе уже рендерят `PersonaAvatar` + имя/роль построчно; тег добавляется рядом с существующей строкой роли, кольцо — на аватар.

**Где используется только кольцо (без тега):** заголовок сообщения персоны в `SessionView` — там уже есть полный текст роли (`{p.name} · {p.role}`), рядом с ним короткий тег-дубликат читался бы избыточно. Кольцо на аватар добавляется, тег — нет.

**Где не используется ни то, ни другое:** маленькие аватары в стеке (`AvatarStack`, `size="sm"`/`"xs"`) — на таком размере кольцо+инициалы нечитаемы, там остаётся текущий плоский вид.

## 4. Пузыри сообщений: заливка цветом персоны

В `SessionView` групповая обёртка сообщений персоны (сейчас `<div className="flex gap-3">`, оборачивает аватар + все пузыри группы) получает `style={personaColorVars(p)}` — один раз на группу, а не на каждый пузырь. Сам пузырь меняет фон с `bg-card` + `border-l-4` на `PERSONA_TINT_CLASS` + `PERSONA_BORDER_CLASS` (обычная рамка со всех сторон на 100% непрозрачности — почему не тонированная рамка см. §2 — без акцента только слева; левая полоска была компенсацией за монохромный фон, теперь фон сам несёт цвет):

```tsx
<div key={group.items[0].id} className="flex gap-3" style={personaColorVars(p)}>
  <PersonaAvatar initials={p.initials} ringClassName={PERSONA_BORDER_CLASS} style={{ backgroundColor: p.hex }} />
  <div className="min-w-0 flex-1 space-y-1">
    {/* ...заголовок группы без изменений... */}
    {group.items.map((m) => (
      <MessageBubble
        key={m.id}
        variant="entity"
        accentClassName={cn(PERSONA_TINT_CLASS, PERSONA_BORDER_CLASS)}
      >
        {m.text}
      </MessageBubble>
    ))}
  </div>
</div>
```

`PERSONA_TINT_CLASS`/`PERSONA_BORDER_CLASS` наследуют `--persona-color`/`--persona-color-dark` от родительского `style` — переопределять их на каждом пузыре не нужно. Пузырь пользователя не меняется — он и так уже использует тот же паттерн (`bg-primary/6` + `border-primary/30`), просто с фиксированным primary вместо персонального цвета.

**Тёмная тема:** заливка (`PERSONA_TINT_CLASS`, 10% непрозрачности) использует `var(--persona-color)` — светлый `hex` из §1, не `darkHex` — аудит подтвердил, что при такой низкой непрозрачности заливка не несёт идентичности сама по себе (это фон, не переключатель контраста) и текст поверх неё остаётся читаемым (~15:1) независимо от темы. Рамка (`PERSONA_BORDER_CLASS`, 100%) — единственное место, где тема действительно меняет цвет, и только для `platform`/`transform`, у которых есть `darkHex`; у остальных 10 персон `dark:border-[var(--persona-color-dark)]` резолвится в тот же `hex`, что и в светлой теме (`darkHex ?? hex` в `personaColorVars`).

## 5. Раскладка: карточка-обложка и сайдбар

### Обложка чата

Текущий блок «Кейс» (плоская карточка с summary) и новый блок шапки чата (стек аватаров + название + «N участников») объединяются в один блок — компонент `ConversationCover` в `council.tsx`:

```tsx
function ConversationCover({ session }: { session: CouncilSession }) {
  const colors = session.personaIds.slice(0, 3).map((id) => getPersona(id).hex);
  const anchors = ["0% 0%", "100% 0%", "50% 100%"];
  const backgroundImage = colors
    .map((hex, i) => `radial-gradient(60% 90% at ${anchors[i]}, color-mix(in oklab, ${hex} 18%, transparent), transparent 65%)`)
    .join(", ");

  return (
    <div className="rounded-2xl border border-border p-4" style={{ backgroundImage, backgroundColor: "var(--color-card)" }}>
      <AvatarStack personaIds={session.personaIds} size="lg" />
      <h2 className="mt-3 text-lg font-bold text-foreground">{session.topic.title}</h2>
      <p className="mt-1.5 text-sm leading-relaxed text-muted-foreground">{session.topic.summary}</p>
      <p className="mt-2 text-xs text-muted-foreground">{session.personaIds.length}{" "}{session.personaIds.length === 1 ? "участник" : "участника"} · на связи</p>
    </div>
  );
}
```

Технически это тот же приём, что уже используется в фоне `<body>` (`styles.css:230-234`, два `radial-gradient` с `color-mix(in oklab, ...)`), просто с цветами персон вместо `--primary`/`--accent` и до трёх точек вместо двух. `AvatarStack` получает новый проп `size` (по умолчанию `"sm"`, здесь — `"lg"`) для крупного отображения в обложке.

Заменяет собой существующий отдельный «шапка чата» блок и «Кейс»-карточку в `SessionView` — один компонент вместо двух.

### Сайдбар

`SessionRow` (`council.tsx`) получает тонкую цветную подложку от первой персоны сессии вместо плоского `hover:bg-secondary/50`:

```tsx
const accentHex = getPersona(session.personaIds[0]).hex;
// на wrapper-div строки:
style={{
  backgroundColor: `color-mix(in oklab, ${accentHex} 6%, var(--color-card))`,
  borderColor: `color-mix(in oklab, ${accentHex} 25%, transparent)`,
}}
```

Один цвет (не градиент из всех участников) — строка маленькая, многоцветный градиент на такой площади читался бы как шум, а не акцент. Стек аватаров (`AvatarStack`) в строке уже существует, увеличивать не нужно — новизна тут только в цветной подложке.

## 6. Движение

### Появление сообщений

Новый keyframe в `src/styles.css` (рядом с существующими `@utility shadow-soft` и т.п.):

```css
@keyframes message-pop {
  0% { opacity: 0; transform: translateY(10px) scale(0.92); }
  60% { opacity: 1; transform: translateY(0) scale(1.04); }
  100% { transform: scale(1); }
}

@utility animate-message-pop {
  animation: message-pop 0.4s cubic-bezier(0.34, 1.56, 0.64, 1);
}
```

Заменяет текущий `animate-in fade-in slide-in-from-bottom-2 duration-300` на всех message-обёртках в `SessionView` (и персон, и пользователя). `prefers-reduced-motion: reduce` уже глобально зануляет `animation-duration` (`styles.css:258-263`) — работает без изменений.

### Пульсация во время печати

Индикатор «печатает» (три подпрыгивающих точки, уже реализован) получает пульсацию кольца на аватаре той же персоны:

```css
@keyframes typing-ring-pulse {
  0%, 100% { box-shadow: 0 0 0 0 color-mix(in oklab, var(--pulse-color, currentColor) 50%, transparent); }
  50% { box-shadow: 0 0 0 6px color-mix(in oklab, var(--pulse-color, currentColor) 0%, transparent); }
}

@utility animate-typing-ring {
  animation: typing-ring-pulse 1.2s ease-out infinite;
}
```

На `PersonaAvatar` типирующей персоны — `className="animate-typing-ring" style={{ "--pulse-color": typingPersona.hex }}`. CSS-переменная нужна, т.к. цвет персоны рантайм-значение, а `@utility` — статическое правило.

### Реакции — анимация появления пилюли

При добавлении реакции сама пилюля появляется через тот же `animate-message-pop` (переиспользуется, отдельный keyframe не нужен).

## 7. Реакции на сообщения

### Данные

`CouncilChatMessage` (`src/data/council.ts`) получает новое опциональное поле:

```ts
export interface CouncilChatMessage {
  id: string;
  author: "user" | string;
  text: string;
  time: string;
  replyTo?: string;
  /** Эмодзи, которыми пользователь отреагировал на это сообщение персоны. */
  reactions?: string[];
}
```

Опциональное поле — обратной несовместимости со storage-схемой v3 нет, бампать ключ не нужно.

`useCouncilSessions` (`src/hooks/useAppState.ts`) получает новый метод:

```ts
const toggleReaction = useCallback(
  (sessionId: string, messageId: string, emoji: string) =>
    setSessions((prev) =>
      prev.map((s) =>
        s.id !== sessionId
          ? s
          : {
              ...s,
              messages: s.messages.map((m) => {
                if (m.id !== messageId) return m;
                const active = m.reactions ?? [];
                const next = active.includes(emoji)
                  ? active.filter((e) => e !== emoji)
                  : [...active, emoji];
                return { ...m, reactions: next };
              }),
            },
      ),
    ),
  [setSessions],
);
```

Возвращается из хука вместе с существующими `addMessages`/`updatePersonas`/`remove`.

### UI

Фиксированный набор — 4 эмодзи: `👍 🤔 😮 🔥`. Никакого открытого пикера.

- Панель реакций видна **только у сообщений персон** (не у сообщений пользователя — реагирует только пользователь, персоны друг на друга и на пользователя в этой итерации не реагируют).
- Триггер появляется по hover на пузырь сообщения (на touch-устройствах — по тапу на сообщение, второй тап на эмодзи подтверждает выбор; реализация через тот же `group-hover` паттерн, что уже используется для кнопки удаления сессии в `SessionRow`).
- Уже активные реакции (пользователь уже нажал) отображаются постоянно под пузырём в виде мелких пилюль — не только на hover.
- Клик по эмодзи (активному или нет) — `toggleReaction(session.id, message.id, emoji)`.
- `aria-pressed` на каждой эмодзи-кнопке отражает состояние, `aria-label` — например `"Отреагировать 👍"`.

## 8. Тестирование и проверка

- Существующий guardrail-тест на реальные имена не трогается — новые поля (`hex`, `darkHex`, `tag`, `reactions`) не текстовые генераторы.
- Новый тест в `council.test.ts`: каждая персона имеет непустой валидный `#rrggbb` в `hex` и непустой `tag`; если задан `darkHex`, он тоже валидный `#rrggbb`.
- Новый тест (лок контрастных значений из таблицы §1, чтобы будущая правка цвета не тихо провалила порог): для каждой персоны — контраст белого текста на `hex` ≥ 4.5:1, и контраст `darkHex ?? hex` против `#101b2c` (тёмная `--card`) ≥ 3:1. Тот же метод расчёта (OKLCH → sRGB → относительная светлота → WCAG-контраст), что уже используется в проекте для `--success`/`--destructive`.
- Новый тест: `toggleReaction` добавляет и убирает эмодзи из `message.reactions`, не трогая остальные сообщения сессии.
- Ручная проверка в браузере (обе темы): аватары с кольцом+тегом в списке выбора персон и в чате — рамка/кольцо `platform` и `transform` заметно светлее в тёмной теме, чем в светлой (единственные две персоны с видимой разницей); тонированные пузыри персон читаемы в обеих темах; обложка чата с градиентом на сессиях с 1/2/3 участниками; сайдбар с цветной подложкой; pop-анимация новых сообщений; пульсация кольца во время «печати»; добавление/снятие реакции переживает reload страницы; совет из `founder`+`industrialist`+`competitor` (три из четырёх переоттеноченных персон) визуально различим с первого взгляда, не «все оранжевые».
- `prefers-reduced-motion: reduce` — обе новые анимации (`message-pop`, `typing-ring-pulse`) должны схлопываться в мгновенное появление/отсутствие пульсации (уже покрыто глобальным правилом в `styles.css`, проверить, что оно применяется к `@utility`-анимациям так же, как к `animate-in`).

## Принятые решения (для справки)

1. Общее направление — «яркое и игривое» (в духе Telegram/Discord), а не премиальное-минималистичное или тёплое-иллюстративное.
2. Идентичность персон — вариант C: кольцо-акцент вокруг аватара + текстовый тег роли под именем (не эмодзи-бейдж).
3. Пузыри сообщений — вариант B: лёгкая заливка цветом персоны (~10-12%) + рамка того же цвета (не плоский нейтральный фон, не полностью закрашенный пузырь).
4. Раскладка — объединение карточки кейса и шапки чата в цветную «обложку» с градиентом из цветов участников; сайдбар — строки сессий со стеком аватаров и цветной подложкой.
5. Движение — вариант B: пружинное появление сообщений + пульсация кольца во время печати + эмодзи-реакции на сообщения.
6. Реакции — сохраняются в данные сессии (переживают reload), а не эффемерная анимация без сохранения.
7. *(добавлено после аудита)* Рамка/кольцо персоны — всегда 100% непрозрачности вместо тонированной 35%, т.к. ни один тон в тонированном виде не держит WCAG 3:1 на тёмной карточке ни при какой разумной непрозрачности. `founder`/`industrialist`/`competitor`/`brand` получили новые `hex` (развёрнутые по оттенку, были скучены в одной 32°-полосе); `platform`/`transform` получили `darkHex` только для рамки в тёмной теме, т.к. их светлота недостаточна для 3:1 при сохранении оттенка.
